@model IEnumerable<Research.Models.About>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ULayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AboutID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AboutID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AboutID })
        </td>
    </tr>
}

</table>
